import React from "react";
import { Box } from "@mui/material";

const LoginHeader = () => {
  return (
    <Box
      sx={{
        display: "flex",
        justifyContent: "center",
        fontSize: "30px",
        mt: 4
      }}
    >
        Username
    </Box>
  );
};

export default LoginHeader;